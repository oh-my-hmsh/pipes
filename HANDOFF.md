# registry — HANDOFF

`flux install` が引く**配布 registry**（`github.com/flux-tools/registry`）。
⭐ **`flux-tools` が焼く場所、ここは配る場所。**

> 📌 使い方は `README.md`。**ここは「なぜそうなっているか」と、踏んだ穴。**
> 📌 マニフェスト形式の契約は `flux-protocol` の `spec/PIPE_MANIFEST_v1.md`。

---

## 🚨 生成物と、手で書いてよい物

| | 誰が作るか |
|---|---|
| `<id>/pipe.json` | **人が書く**（投稿の単位はディレクトリ 1 つ）|
| `<id>/<platform>/<bin>` | 焼いた実体を置く |
| **`pipes.json`** | 🚨 **生成物**（`./build-catalog.py`）。手で編集すると CI で落ちる |

### ⭐ カタログが生成物である理由（2026-08-19 の実測）

手書きだった間に **3 つ**壊れていた: ①載っているのに実体が無い「幽霊」2 件
②同じ `description` が実体とカタログで別の文 ③複製が別リポにも在り、org 改名が
片方にしか当たっていなかった。⭐ 生成にすると、この 3 つは**構造的に起きなくなる**。

---

## 🚨 実体から導出する 2 つの欄（**手で書く欄にしない**）

```json
"build_ids":       { "linux-x86_64": "ed68217e1", ... }   ← 各面のバイナリに埋まった版
"manifest_digest": "cff2dee212ae878a"                      ← pipe.json のバイト列の指紋
```

⭐ どちらも `build-catalog.py` が**実体を読んで**載せる。
🚨 手で書く欄にすると、**同じ事実が 2 箇所**になり、焼き直した回に片方だけ動いて必ず腐る
—— **そして腐った側は「最新です」と言い続ける**。

### ⚠️ なぜ `version` 欄ではないのか

📏 2026-08-26 の実測: 配っている 10 本のうち **4 本が `version` を持たないのに、
その実体は `1.0.0` / `3.0.0` を名乗っていた** ＝ 宣言と実体が既に食い違っていた。
⭐ 対して build id は焼くたびに必ず入る。

### 🚨 指紋は正規化しない

用事は「**同じ物が置かれたか**」であって「意味が同じか」ではない。
⚠️ 正規化規則が 2 実装に分かれた瞬間、**同じ宣言が違う指紋になり、
全台が「宣言が古い」と言われ続け、入れ直しても直らない**。

---

## 🚨 配る手前の門は 3 つ（CI が全部撃つ）

```sh
./build-catalog.py --check      # カタログが実体と一致しているか（バイト列の完全一致）
./check-artifacts.py            # 宣言した面の実体が在り・linux 面が静的・全部が版を名乗るか
./check-digest-agreement.py     # 指紋の計算が python ↔ rust で一致しているか
```

### ⭐ `check-artifacts.py` —— **何も測らなかったときに緑を返さない**

母集団が空なら違反も 0 件になる。∴ **数えた実体が 0 個なら非ゼロで終わる**。
⚠️ 静的の母集団（`examined`）と版の母集団（`stamped`）は**別に数える** ——
一本化すると「版を 1 つも読まなかった」が緑に化ける。

⚠️ 静的判定に **`ldd` も `file` も使わない**（言葉で答える道具で、綴りが違う）。
ELF のプログラムヘッダを直接読み、`PT_INTERP` の有無で決める。

### 🚨 `check-digest-agreement.py` —— **一致だけでは足りない**

指紋の計算は **2 言語に分かれている**（配る側 Python / 入れる側 Rust）。⚠️ 言語が違うので
1 本化できない。⭐ **初回から実バグを捕まえた** —— Rust 側の FNV 素数が
`0x1000_0000_01b3`（ゼロ 1 つ多い）で、🚨 **下位 32bit は一致したまま**だったので
目では気づかなかった。

∴ 両側を**公開されている FNV-1a のテストベクタ**にも釘付けにしている ——
「相手と一致していればよい」だと、**2 実装が同じように間違ったとき**に気づけない。

⚠️ 使う `flux` は**リポの中の実体**（`flux/linux-x86_64/flux`）。
⭐ 配ろうとしている物そのもので測るのが正しく、CI に依存も増えない。

---

## 🚨 CDN は 1 つの真実を返さない

### ① 一覧と実体は**別のオブジェクト**

📏 実測（2026-08-26）: カタログが新版を名乗ってから実体が追いつくまで **25〜120 秒**。
その窓で `flux update` は「古い」と言って**古い実体を落とし、『✅ 入りました』と言い、
次もまた『古い』と言う** ＝ **直らない更新**。

### ② 🚨 同じ URL でも、**頼み方ごとに別の実体**

```
curl                             → 26bdbd9d6   （新しい）
curl -H 'Accept-Encoding: gzip'  → 6f1fdfc15   （古い）
```

📏 **ズレは 125 秒**（identity 120 秒 / gzip 245 秒）。ETag も別物。
⚠️ `Cache-Control: no-cache` / `Pragma` / クエリ文字列の**どれでも剥がせない**。

⭐ ∴ **publish 直後の検算は、両方の実体で行う**（片方だけ見ると収束したと誤読する）。
⭐ クライアント側は `Accept-Encoding: identity` で実体を 1 つに寄せ、
**落とした物の版をカタログの約束と突き合わせる**（`flux` が実装済み）。

---

## 📌 住所の履歴（⚠️ 転送は効くが、それが罠でもある）

```
oh-my-hsh/pipes  →  oh-my-hmsh/pipes  →  flux-tools/pipes  →  flux-tools/registry
```

📏 **4 通りとも 200 を返し、同じ中身が降ってくる**（2026-08-26 実測）。
⭐ ∴ 配備済みの台は改名で壊れない。
🚨 **だからこそ、旧住所を焼いたまま何年も気づかない**ことが起こりうる ——
`flux` 側は `registry.rs::the_default_registry_is_the_new_address` で釘付けにしている。

⚠️ **`oh-my-hmsh/themes` は別物**（hmsh のテーマ）。ここには関係しない。

---

## ⚠️ 名前について

- **リポ名 `registry`**（2026-08-26 に `pipes` から改名）—— 叩く面は **4 つ**
  （MCP / シェルの `|` / hmsh チェーン / 直叩き）で、`pipe` は**そのうち 1 面**の呼び名だった。
  📏 MCP は宣言を使わず `spawnSync` でバイナリを名前で起動している。
- ⚠️ **中身の語はまだ `pipe`**（`pipes.json` / `pipe.json`）——
  📏 hsh だけで 1999 行。⭐ **語ごと替えるのは別案件**。
